
locals {
  appinsightsname = "${var.resource_type_application_insights}-${var.concat_company}-${var.concat_department}-${var.concat_projectname}-${var.concat_environment}"
}

# appi-kii-es-mediaservice